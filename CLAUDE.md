# CLAUDE.md

## プロジェクト概要
このプロジェクトはcc-sdd（仕様駆動開発）を用いて開発を進めます。

## 開発スタイル
- 仕様書（spec）を先に作成し、承認後に実装する
- 仕様: `.specs/` ディレクトリに格納
- 実装前に必ず要件・設計の承認を得ること

## コーディング規約
- コメントは必要最小限に留める（WHYが非自明な場合のみ）
- 変数・関数名は意図が明確になるよう命名する
- セキュリティ: ユーザー入力・外部APIの境界でのみバリデーション

## GitHub Actions での @claude の使い方
- `@claude このIssueを実装してPRを作成して` — 実装依頼
- `@claude このPRをレビューして` — コードレビュー依頼
- `@claude バグを修正して` — バグ修正依頼
